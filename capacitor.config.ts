import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.biblegames.eden',
  appName: 'Eden Choice Chronicles',
  webDir: 'dist',
  ios: {
    contentInset: 'automatic'
  }
};

export default config;
